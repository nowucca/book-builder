[project]
name = "book-builder"
version = "1.0.0"
description = "Reusable book publishing system with Python dependencies"
requires-python = ">=3.9"
dependencies = [
    "pygments>=2.17.0",
]

[project.optional-dependencies]
dev = [
    "pygments>=2.17.0",
]
